# Public backend address, NOT a secret. Restart Vite after editing.
# Empty = explicitly labelled local demo data; no score or AI calls.
VITE_API_BASE_URL=

# Set ONLY after Member 2 confirms actual routes. There are no route defaults.
VITE_BOOTSTRAP_PATH=
VITE_SIMULATE_PATH=
VITE_ANALYZE_PATH=

# Never put OPENAI_API_KEY, NVIDIA_API_KEY or other secrets in this frontend.
